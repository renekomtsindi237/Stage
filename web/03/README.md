Dossier `03` contenant le `src` unique pour repartir sur une base propre.

Contenu:
- src/index.html
- src/main.ts
- src/styles.scss
- src/environments/environment.ts
- src/app/* (module, routing, components, features)

Prochaine étape: si vous voulez, je peux:
- supprimer ou renommer les anciens dossiers `src`, `src_simple`, `src_skeleton` (répondre "nettoyer"),
- ou laisser les dossiers en place (répondre "laisser").
