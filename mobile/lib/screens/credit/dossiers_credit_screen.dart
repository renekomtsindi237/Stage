import 'package:flutter/material.dart';

class DossiersCreditScreen extends StatelessWidget {
  const DossiersCreditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dossiers crédit')),
      body: const Center(child: Text('Dossiers crédit — à implémenter')),
    );
  }
}
