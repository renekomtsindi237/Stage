import 'package:flutter/material.dart';

class RecouvrementListScreen extends StatelessWidget {
  const RecouvrementListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Recouvrement')),
      body: const Center(child: Text('Liste recouvrement — à implémenter')),
    );
  }
}
