import 'package:flutter/material.dart';

class MesDelegationsScreen extends StatelessWidget {
  const MesDelegationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mes délégations')),
      body: const Center(child: Text('Délégations — à implémenter')),
    );
  }
}
