import 'package:flutter/material.dart';

class HistoriqueJourScreen extends StatelessWidget {
  const HistoriqueJourScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Historique du jour')),
      body: const Center(child: Text('Historique — à implémenter')),
    );
  }
}
