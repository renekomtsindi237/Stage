# Dépendances minimales pour le CI pytest (tests/).
# Les Dockerfiles (Dockerfile.ml, Dockerfile.airflow) installent leurs propres
# dépendances lourdes (airflow, dbt, fastapi, scikit-learn…).
pandas==2.2.2
psycopg2-binary==2.9.9
pytest==8.1.1
pytest-cov==5.0.0
